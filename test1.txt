EXEC -------------
exec:cat
$PATH:/usr/local/opt/llvm/bin:/Users/greed/.cargo/bin:/Library/Frameworks/Python.framework/Versions/3.8/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/Library/Apple/usr/bin:/usr/local/opt/llvm/bin:/usr/local/opt/fzf/bin
path:/usr/local/opt/llvm/bin
path:/Users/greed/.cargo/bin
path:/Library/Frameworks/Python.framework/Versions/3.8/bin
path:/usr/local/bin
path:/usr/bin
path:/bin
	f:cat 8 8 1
pathname:/bin/cat
excve pathname_/bin/cat
Ctrl-D : exit
$? : return value most recent function
Ctrl-\ : kill forground process
| : PIPE
; : do stuff
redir : fuck this shit
fix leaks




File REDIRECTION:
>
takes std input and writes it to a file, over writes existing
	lines, openes a buffer to write into and closes with CTRL_D
>>
appends input to file
<
dup std:in
open file
dup2 to->std:in
restore fd
close


export = check to see if name is used in env name
if it is not add it to the struct list
	re attach the struct list pointer back to the struct
	if the name is already set free (val) and reassign whats being
		instructed to that



NOTES : PARSER NEEDS TO BE ADJUSTED TO STORE FROM $->$ || \0 || ' '
SANDER:
 finished ---> pwd


GRIFFIN:
 finished ---> exit
 finished ---> echo (need to do -n)



ORDER
1st
ENV
 finished ---> EXIT
ECHO
 finished ---> PWD

LATER
EXPORT
UNSET
CD

LAST
PIPES
';'
REDIRECTION
[1;34m>>: [0m